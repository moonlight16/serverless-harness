{
  "spec": "docs/specs/2026-09-09-p4-microvm-sandbox-design.md",
  "section": "7.4",
  "recordedAt": "2026-09-10",
  "note": "Written BEFORE the first rung. §7.4 says 'the four below' and then lists five; all five are recorded here, because the count is a drafting artifact and dropping one would be exactly the reinterpretation the pin exists to prevent.",
  "predictions": [
    {
      "id": 1,
      "claim": "Replenishment binds on process/memory count before CPU.",
      "metric": "host MemAvailable + Mlocked + process count vs host CPU utilisation at the knee",
      "falsifiedBy": "CPU saturating at or before the memory gate first refuses",
      "onlyObservableBecause": "§6's memory gate exists; without it the prediction would be 'confirmed' by the host falling over, which is not a measurement"
    },
    {
      "id": 2,
      "claim": "The warm hot path lands within 2x of the container baseline, because both pay the relay hop and it dominates.",
      "metric": "E10 rung 2 p50 / E10 rung 1 p50",
      "falsifiedBy": "ratio > 2 on metal"
    },
    {
      "id": 3,
      "claim": "The knee is a replenishment knee, not a latency knee: cold-acquire rate stays approximately 0 until replenishment rate meets Exec rate, then rises sharply.",
      "metric": "cold-acquire rate vs active runs across E11's rungs",
      "falsifiedBy": "cold-acquire rate rising gradually from the first rung, or p95 degrading while cold-acquire rate stays flat",
      "note": "This predicts the knee's SHAPE, a stronger commitment than a number."
    },
    {
      "id": 4,
      "claim": "Cloud Hypervisor's virtio-fs costs more per metadata op than Firecracker's block, visible in ls/find-heavy commands rather than in cat.",
      "metric": "per-command p50 by command class, both arms, E10 rung 2",
      "falsifiedBy": "no arm difference on ls/find, or a difference of the same size on cat"
    },
    {
      "id": 5,
      "claim": "Idle standby residency returns to zero within StandbyIdle + ReclaimScanInterval of a rung's last Exec on an otherwise idle host, while workspace count does not change until WorkspaceIdle; and because a run's final Exec pops a standby and mints its replacement, the run's full complement of D stands idle rather than being reclaimed, so idle standby residency tracks (runs finishing per StandbyIdle) x D x GuestRAMBytes and not zero.",
      "metric": "idle standby residency and workspace count, sampled after each rung's last Exec",
      "falsifiedBy": "standbys outliving StandbyIdle + ReclaimScanInterval on an idle host, workspaces vanishing before WorkspaceIdle, or residency reading zero rather than (runs finishing) x D",
      "note": "Predicts the reclamation path's SHAPE — convergence with no arrival to trigger it, and RAM released long before state is."
    },
    {
      "id": 6,
      "experiment": "E12",
      "claim": "Guest-initiated vsock on a second port (1025) works on a fresh boot and survives snapshot restore, for all N concurrent restores of one snapshot, because the guest-to-host direction needs no handshake and no host-side state beyond the socket file - strictly less state to reset than the host-initiated direction already known to survive.",
      "metric": "per-rung boolean: the VM's unique nonce captured host-side on <jail>/vsock.sock_1025 AND 'ACK <nonce>' returned in that VM's guest stdout, for rungs A, B, C(N=8), C(N=128), D",
      "falsifiedBy": "any rung B, C or D reporting ok=false while rung A reported ok=true",
      "note": "Rung A failing falsifies nothing about Firecracker - it indicts our own plumbing or socket naming, and the probe is retried. Recorded before rung A ran, per issue #271."
    },
    {
      "id": 7,
      "experiment": "E13",
      "claim": "PR #272's rung C@128 connection failures are caused by host memory exhaustion under 128x256MiB guest RAM demand against nested-m8i's 15 GiB total (per issue #266's own admission-budget finding on this rig class), not by a defect in the guest-initiated vsock-across-restore mechanism E12 added.",
      "metric": "e13-restore-capacity-control.sh's control-128.json fail_count (existing 1024 Exec only, no second port) compared against PR #272's rung-C-128.json fail_count; and e13-correlate.py's failed_near_oom_event / failed_below_low_water counts from a telemetry-wrapped rerun of rung C@128",
      "falsifiedBy": "the control script's fail_count at N=128 is close to zero while PR #272's own rung C@128 fail_count stayed high, OR the telemetry rerun shows no OOM-killer activity and MemAvailable never drops near the control script's failures' timestamps",
      "note": "If the control script (no second port at all) reproduces a similar failure rate to PR #272's rung C@128, that alone falsifies any claim that E12's mechanism is implicated - the fix belongs to concurrent-restore capacity, not to vsock-across-restore."
    }
  ]
}
